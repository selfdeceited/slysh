import { css } from "emotion";

export const main = css`
  text-align: left;
  padding: 1rem;
  max-width: 280px;
  margin: 0 auto;
  min-width: 150px;
`;

export const devInfo = css`
  color: darkred;
  text-align: right;
  font-size: 0.8rem;
`;
